databaseChangeLog:

  # 目录由 spring.liquibase.parameters.changelogDir 决定：
  #   dev  -> h2
  #   prod -> pg
  # 以前是靠手工注释/放开两段 includeAll 来切换，容易漏改，
  # 也没法让同一份构建产物在两种库上跑。
  - includeAll:
      path: ${changelogDir}/
      relativeToChangelogFile: true
