description = "backend-parent"

subprojects {
    apply(plugin = "java")
    apply(plugin = "org.springframework.boot")
//    apply(plugin = "org.hibernate.orm")
//    apply(plugin = "org.graalvm.buildtools.native")
}
